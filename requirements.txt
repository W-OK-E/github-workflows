# ResLocoTransformer — Python dependencies
#
# PyTorch must be installed separately with the correct CUDA/CPU variant.
# See https://pytorch.org/get-started/locally/ for the right install command.
# Example (CUDA 12.1):
#   pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu121
# Example (CPU only):
#   pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu

# ── Core (required for training) ────────────────────────────────────────────
numpy>=1.21.0
gym>=0.21.0
mujoco>=3.0.0          # MuJoCo Python bindings (DeepMind)
tensorboardX>=2.6.0    # Training logger (TensorBoard-compatible)
tabulate>=0.9.0        # Pretty-print training stats in terminal
toolz>=0.12.0          # dicttoolz.merge_with used in VecEnv / SubProcVecEnv
posix_ipc>=1.1.0       # Shared-memory replay buffer (Linux only)
torch==2.11.0
opencv-python==4.13.0.92
# ── Optional: plotting & log analysis ───────────────────────────────────────
matplotlib>=3.5.0
seaborn>=0.12.0
